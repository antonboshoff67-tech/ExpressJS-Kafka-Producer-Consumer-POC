# Copy this file to backend-secret.yaml, fill in real values, and DO NOT commit
# backend-secret.yaml to source control (it is already covered by .gitignore
# patterns for *-secret.yaml if you add one; double-check before committing).
apiVersion: v1
kind: Secret
metadata:
  name: item-kafka-backend-secret
  namespace: item-kafka-poc-express
type: Opaque
stringData:
  ITEM_MYSQL_SOURCE_USERNAME: "item_poc_user"
  ITEM_MYSQL_SOURCE_PASSWORD: "change-me-strong-password"
  ITEM_MYSQL_USERNAME: "item_poc_user"
  ITEM_MYSQL_PASSWORD: "change-me-strong-password"
  ITEM_JWT_PRIVATE_KEY: ""

